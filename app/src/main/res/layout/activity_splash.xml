<?xml version="1.0" encoding="utf-8"?>
<layout>

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                    xmlns:app="http://schemas.android.com/apk/res-auto"
                    android:id="@+id/splash_container"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout_gravity="center"
                    android:background="@color/splash_screen"
                    android:fitsSystemWindows="false">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:orientation="vertical">

                <ImageView
                    android:id="@+id/splash_image_view_logo"
                    android:layout_width="@dimen/default_splash_logo_width"
                    android:layout_height="@dimen/default_splash_logo_height"
                    android:contentDescription="@string/app_name"
                    app:srcCompat="@drawable/vd_splash_logo"/>

                <ImageView
                    android:id="@+id/splash_image_view_name"
                    android:layout_width="@dimen/default_splash_logo_width"
                    android:layout_height="@dimen/default_splash_logo_height"
                    android:contentDescription="@string/app_name"
                    app:srcCompat="@drawable/favicoin"/>

            </LinearLayout>

            <TextView
                android:id="@+id/version_text_view"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentBottom="true"
                android:layout_centerHorizontal="true"
                android:layout_marginBottom="@dimen/dialog.margin.xxl"
                android:textColor="@color/grey_df"
                android:textSize="@dimen/text.size.s"
                android:textStyle="normal"/>

        </RelativeLayout>

</layout>